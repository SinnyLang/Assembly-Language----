实验七

问题描述大概是：将一堆数据按照顺序复制到一个叫做tables的内存区段

问题思路：
    根据书中提示，将tables看作一个结构体，利用bx指向第一个结构体，利用idaata常数指向结构体中的元素，如果利用si做游标，逐字节复制数据
      bx          -> tables[0]
      bx+idata    -> tables[0].year
      bx+idata+si -> tables[0].year[0]
    结构体的寄存器已经分配完了，接下来源数据分配寄存器。利用es寄存器做该区段的基地址
