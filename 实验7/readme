实验七

问题描述大概是：将一堆数据按照顺序复制到一个叫做tables的内存区段

问题思路：
    根据书中提示，将tables看作一个结构体，利用bx指向第一个结构体{'year summ ne ?? '}，利用idaata常数指向结构体中的元素，如果利用si做游标，逐字节复制数据
      bx          -> tables[0]
      bx+idata    -> tables[0].year
      bx+idata+si -> tables[0].year[0]
    结构体的寄存器已经分配完了，接下来源数据分配寄存器。因为该区段不能看做一个结构体，所以不能使用结构体的那一套分配方案。这里利用es寄存器做该区段的基地址，di作为数据字节的偏移地址，idata作为各类不同数据的偏移地址。
      es+di       -> year[0]
      es+idata+di -> summ
      es+idata+di -> ne
    首先是复制年份字符串，利用一个循环，将字符串每两字节的复制过去，由于8086寄存器不支持内存单元之间的复制，所以将寄存器ax做中介复制过去。这里使用了双层循环，要注意cx压栈。
    然后是复制公司人数，如果这里使用di寄存器复制的话，需要进行di/2-1的复杂运算，这里就使用了寄存器bp。
      es+bp       -> ne
    最后是复制总收入，首先将dx后退2。为了方便进行除法运算，将高两位使用dx做中介，第两位仍然使用ax做中介进行复制。
    然后是除法运算人均收入，复制过去。
    
