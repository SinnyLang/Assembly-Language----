实验6的第二题
（2）完成书中7.9的问题
问题描述：
    问题描述大致是将data段中4个英文单词中，每个单词的前四个字母由小写替换为大写

问题思路：
    按照书中的分析，将data段存放在ds中，将bx基寄存器指向第一个字符串，游标寄存器si归零，至于如何将字符串从第三个开始则利用常量标注，最后字符的表示形式为[bx+3+si]
    转换过程中使用al储存单个字符，经过与dfH进行逻辑与运算即可转换为大写
    因为字符是二维数组，所以在遍历修改的时候需要使用嵌套循环，在每层循环之前，需要将cx入栈，每层循环结束之后将外层循环的cx从栈中恢复
